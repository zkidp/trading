# trading

个人交易/量化研究项目骨架。

## 结构
- `src/`：核心代码
- `notebooks/`：研究笔记
- `data/raw/`：原始数据（不入库）
- `data/processed/`：处理后数据（不入库）
- `configs/`：配置文件
- `scripts/`：脚本
- `tests/`：测试

## 快速开始
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
